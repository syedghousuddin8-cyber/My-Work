# NOTE: This is a template. In production, use external secrets management
# like AWS Secrets Manager, HashiCorp Vault, or Kubernetes External Secrets

apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: delivery-platform
type: Opaque
stringData:
  # Database passwords (base64 encode in production)
  postgres.password: "CHANGE_ME_IN_PRODUCTION"
  mongodb.password: "CHANGE_ME_IN_PRODUCTION"
  redis.password: "CHANGE_ME_IN_PRODUCTION"

  # JWT secret
  jwt.secret: "CHANGE_ME_IN_PRODUCTION_LONG_RANDOM_STRING"

  # Third-party API keys
  stripe.secret.key: "sk_test_CHANGE_ME"
  stripe.webhook.secret: "whsec_CHANGE_ME"
  google.maps.api.key: "CHANGE_ME"
  firebase.project.id: "CHANGE_ME"
  firebase.client.email: "CHANGE_ME"
  firebase.private.key: "CHANGE_ME"
  twilio.account.sid: "CHANGE_ME"
  twilio.auth.token: "CHANGE_ME"
  twilio.phone.number: "CHANGE_ME"
  sendgrid.api.key: "CHANGE_ME"
  sendgrid.from.email: "noreply@delivery.com"
